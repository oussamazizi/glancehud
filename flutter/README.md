# GlanceHUD — Flutter plugin

Dart API over the C ABI via `dart:ffi`. Overlay can be hosted natively or drawn
in Dart from the metric stream.

_Scaffold pending — see the roadmap in the root README._
