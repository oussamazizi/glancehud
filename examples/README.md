# GlanceHUD — Examples

Runnable sample apps demonstrating GlanceHUD integration:

- `android/` — native Android app
- `ios/` — native iOS app
- `flutter/` — Flutter app using the plugin
- `react-native/` — React Native app using the bridge

_Scaffold pending — added alongside each platform module._
