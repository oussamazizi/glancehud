# GlanceHUD — iOS module

Objective-C++ bridge (C-ABI → `Api.h`) + Swift wrapper and the native overlay
(transparent `UIWindow`). Platform metric providers (CPU/RAM via mach, GPU via
Metal, network) implement `glance::IMetricProvider`.

_Scaffold pending — see the roadmap in the root README._
